# `docker compose --env-file .env.secret up --build` runs the full stack: the
# bighead release on http://localhost:4000, Postgres, and Ollama for embeddings.
# `docker compose up db` still works for host-side development.
services:
  app:
    build: .
    ports: ["4000:4000"]
    environment:
      DATABASE_URL: ecto://postgres:postgres@db/bighead_dev
      # Local default; override for anything reachable from another machine.
      SECRET_KEY_BASE: ${SECRET_KEY_BASE:-8c7nI6sqeZirCQ+0a20tCWafOrH4UUM6Ih67i7DP3PGJFUIuXeYniM0bJ2DsMICm}
      PHX_HOST: localhost
      BIGHEAD_EMBEDDER_PROVIDER: ollama
      OLLAMA_BASE_URL: http://ollama:11434
      BIGHEAD_LLM_PROVIDER:
      ANTHROPIC_API_KEY:
      OPENROUTER_API_KEY:
      BIGHEAD_LLM_BASE_URL:
      BIGHEAD_LLM_MODEL:
      BIGHEAD_LLM_MAX_TOKENS:
      BIGHEAD_LLM_EFFORT:
      BIGHEAD_EMBEDDING_MODEL:
      BIGHEAD_EMBEDDING_DIMENSIONS:
      BIGHEAD_DEFAULT_USER_ID:
      BIGHEAD_LOG_LLM_PAYLOADS:
    command: ["/bin/sh", "-c", "/app/bin/migrate && /app/bin/server"]
    depends_on:
      db:
        condition: service_healthy
      ollama:
        condition: service_healthy

  db:
    image: pgvector/pgvector:pg18
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: bighead_dev
    ports: ["5432:5432"]
    volumes: [bighead_pgdata:/var/lib/postgresql]
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 10

  ollama:
    image: ollama/ollama:latest
    ports: ["11434:11434"]
    entrypoint:
      - /bin/sh
      - -c
      - |
        ollama serve &
        until ollama ls >/dev/null 2>&1; do sleep 1; done
        ollama pull nomic-embed-text
        wait
    volumes: [bighead_ollama:/root/.ollama]
    healthcheck:
      test: ["CMD", "ollama", "ls"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  bighead_pgdata:
  bighead_ollama:
